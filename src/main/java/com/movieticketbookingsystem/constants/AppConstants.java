package com.movieticketbookingsystem.constants;

public class AppConstants {

    // Defining the max seats per showtime as a constant
    public static final int MAX_SEATS_PER_SHOWTIME = 2;

    // Defining the base price of a movie
    public static final int BASE_MOVIE_PRICE = 50;

    public enum Role {
        ADMIN,
        CUSTOMER
    }

}